# CLAUDE.md — برنچ `medora-ai`

این برنچ **فقط** محصول مدورا است: بیلد توزیع شخصی، **بدون هیچ سیستم لایسنسی**.

مدورا یک محصول مستقل و تک‌محصولی روی این برنچ است. هیچ محصول دیگری اینجا نیست و نباید اضافه شود — نه کد، نه سند، نه دارایی بازاریابی. اگر کاری به محصول دیگری مربوط است، جای آن اینجا نیست.

**قبل از هر کاری `MEDORA.md` را بخوان** — معماری، کارهای اخیر با جزئیاتشان، و روش تست در این محیط آنجاست.

---

## قانون‌های ثابت این بیلد

1. **هیچ سیستم لایسنسی ندارد و نباید اضافه شود.** این عمدی است. `medora/includes/setup.php` فقط `Plugin::start()` را روی `plugins_loaded` صدا می‌زند.

2. **این بیلد برای مارکت‌پلیس نیست.** محدودیت نام تک‌کلمه‌ای، ممنوعیت لینک و استاندارد ارسال فایل فروشگاهی اینجا اعمال نمی‌شود؛ توزیع شخصی است.

3. **هیچ ردی از SignTeb نباید برگردد** — نه در هدر، نه در نام کلاس‌ها، نه در متن‌ها.

4. **علت خطای سرویس هوش مصنوعی هرگز نباید به مرورگر بازدیدکننده برود.** بازدیدکننده فقط همان متن مؤدبانهٔ همیشگی را می‌بیند؛ علت فقط سمت سرور می‌ماند برای هشدار پنل و دکمهٔ تست ارتباط.

5. **کلیدهای API با AES-256-GCM رمزنگاری ذخیره می‌شوند** و فقط وقتی مقدار تازه تایپ شود بازنویسی می‌شوند.

6. **کدهای پترن پیامکی شخصی مالک محصول هرگز نباید داخل افزونه ارسال شود.**

---

## وضعیت

نگارش **۴٫۲٫۰**. پوشه `medora/` · شورت‌کد `[medora_chat]` · گزینه `mdr_settings` · جدول‌ها `{wp_prefix}mdr_*`.

کار باز: مالک محصول باید بعد از نصب، دکمهٔ «تست ارتباط با سرویس هوش مصنوعی» را بزند؛ متن خطا (اگر باشد) قدم بعدی را تعیین می‌کند.

## ساخت بسته

```bash
zip -rq dist/medora-ai.zip medora -x '*.DS_Store'
```

`dist/` در `.gitignore` است و کامیت نمی‌شود.

> ⚠️ ترتیب نصب برای سایتی که نسخهٔ قدیمی (`swc_`) دارد: **اول** مدورا را فعال کنید تا داده‌ها منتقل شوند، **بعد** افزونهٔ قدیمی را حذف کنید. `uninstall.php` قدیمی هنگام حذف جدول‌های `swc_` را DROP می‌کند.
