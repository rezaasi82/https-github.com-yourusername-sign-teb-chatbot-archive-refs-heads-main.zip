# CLAUDE.md — برنچ `pezhkam`

این برنچ **فقط** محصول پژکام است: بیلد مارکت‌پلیس راست‌چین، با سیستم لایسنس.

مدورا اینجا نیست و نباید بیاید. آن یک محصول جداست روی برنچ `medora` با قوانین متضاد (بدون لایسنس، بدون قوانین مارکت‌پلیس).

**قبل از هر کاری `PEZHKAM.md` را بخوان** — تاریخچهٔ نام، سه دور ایراد راست‌چین و پاسخشان، ساختار دارایی‌های صفحهٔ محصول و روش تست در این محیط آنجاست.

---

## 🔴 چیزهایی که شکستنشان محصول را از کار می‌اندازد

1. **`pezhkam/includes/RTL_License_0949e1086a8664d9.php` را باز نکن، ویرایش نکن، فرمت نکن.**
   فایل ionCube-encode شدهٔ مارکت است. یک بایت تغییر، sha1 را می‌شکند و افزونه اصلاً بوت نمی‌شود.
   sha1 معتبر: `7a08b73e61f2c61ca287e1f88650085a47328a54`

2. **`pezhkam/includes/setup.php`** همان فایلی است که برای انکد ionCube به راست‌چین فرستاده می‌شود. گیت لایسنس داخل آن است.

3. **`Plugin::licensed()`** لایهٔ دوم مستقل اعتبارسنجی است. حذفش نکن.

4. **هیچ اطلاعات، راه ارتباطی یا لینک شخصی، در هیچ کجای محصول** — قانون صریح راست‌چین. قبلاً به همین دلیل رد شده‌ایم.

5. **هیچ ردی از نویسندهٔ AI** در فایل‌های منتشرشده. شناسهٔ مدل و لینک session فقط در trailer کامیت گیت.

6. **کدهای پترن پیامکی شخصی مالک محصول هرگز نباید داخل افزونه ارسال شود.**

---

## وضعیت

**در انتظار تأیید راست‌چین.** تصمیم صریح مالک محصول: تا تأیید نشدن، هیچ تغییری روی این محصول داده نشود.

## ساخت بسته‌ها

```bash
zip -rq dist/pezhkam.zip pezhkam -x '*.DS_Store'          # فایل بروزرسان
# پکیج محصول: همین zip + marketing/guide/*
```

`dist/` در `.gitignore` است و کامیت نمی‌شود.
