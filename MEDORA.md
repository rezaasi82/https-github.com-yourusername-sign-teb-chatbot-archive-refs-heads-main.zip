# Medora AI — context

> این فایل برای شروع یک چت مستقل روی «مدورا» است. هر چیزی که برای ادامهٔ کار لازم است اینجاست.
> **پژکام به این محصول ربطی ندارد** — آن یک بیلد جداست با قوانین جدا (فایل `PEZHKAM.md`).

## این چیست

افزونهٔ وردپرس چت هوشمند پزشکی برای **توزیع شخصی خودِ مالک محصول**. روی سایت‌های خودش نصب می‌شود.

- پوشه: `medora/` · نگارش فعلی: **۴٫۲٫۰**
- Namespace: `Medora\` · ثابت‌ها: `MDR_*` · پیشوند CSS: `mdr-`
- گزینه: `mdr_settings` · جدول‌ها: `{wp_prefix}mdr_*` · شورت‌کد: `[medora_chat]`
- Text Domain: `medora` · Author: `Medora AI` · بدون `Plugin URI`

## قانون‌های ثابت این بیلد

1. **هیچ سیستم لایسنسی ندارد و نباید اضافه شود.** این بیلد عمداً بدون گیت فعال‌سازی است. فایل `includes/setup.php` فقط `Plugin::start()` را روی `plugins_loaded` صدا می‌زند.
2. **قوانین راست‌چین به این بیلد اعمال نمی‌شود.** محدودیت‌های مارکت‌پلیس (نام تک‌کلمه‌ای، ممنوعیت لینک، استاندارد ارسال فایل) مال پژکام است، نه این.
3. **هیچ ردی از SignTeb نباید برگردد** — نه در هدر، نه در نام کلاس‌ها، نه در متن‌ها.
4. **کلیدهای API با AES-256-GCM رمزنگاری ذخیره می‌شوند** و فقط وقتی مقدار تازه تایپ شود بازنویسی می‌شوند.
5. **کدهای پترن پیامکی شخصی مالک محصول هرگز نباید داخل افزونه ارسال شود.**

## معماری

- Autoloader سبک PSR-4-مانند: `Medora\Sub\ClassName` → `includes/sub/class-class-name.php`
- ورودی‌ها فقط از `Medora\Core\Input` خوانده می‌شوند؛ هیچ‌جای دیگری به superglobal دست نمی‌زند
- کوئری‌ها با `$wpdb->prepare()` و پارامتر bind‌شده
- ارتباط چت از REST، و اگر هاست REST را ببندد خودکار به `admin-ajax` سوییچ می‌کند
- بارگذاری شرطی asset‌ها: JS/CSS فقط در صفحه‌ای که گفتگو دارد

## کارهای انجام‌شدهٔ اخیر

| کامیت | کار |
|---|---|
| `2a1c4c0` | تفکیک نمایش ویجت به دو کلید مستقل: ویجت شناور، و فعال بودن شورت‌کد |
| `928eb1d` | انتخاب مدل از dropdown هر سرویس‌دهنده + گزینهٔ «مدل دیگر» برای ورود دستی |
| `108fa26` | ریشه‌یابی پیام «فعلاً پاسخگو نیستم» |
| `e585e0d` | حذف `soft_error` از پاسخ سرور؛ علت خطا دیگر به مرورگر نمی‌رود |
| `f15f9bd` | انتقال خودکار نصب قدیمی (`swc_*`) به این بیلد + نگارش ۴٫۲٫۰ |
| — | افزودن Google Gemini به‌عنوان سرویس‌دهندهٔ چهارم |

### جزئیاتی که ممکن است لازم شود

**سرویس‌دهنده‌ها** — چهار تا: Anthropic، OpenAI، GapGPT و Google Gemini. سه‌تای اول پروتکل مشترک دارند؛ Gemini پروتکل خودش را دارد (`contents`، نقش `model` به‌جای `assistant`، `system_instruction` جداگانه) و برای همین کلاس مستقل `ProviderGemini` است، نه زیرکلاس.

**انتخاب مدل** — کاتالوگ مدل‌ها در `ProviderFactory::models_for()` است. این فهرست **محدودکننده نیست**: گزینهٔ `__custom__` یک فیلد متنی باز می‌کند و هر شناسه‌ای قابل ورود است. `SettingsPage::chosen_model()` مقدار پست‌شده را resolve می‌کند و فیلد خالی را به پیش‌فرض همان سرویس برمی‌گرداند.

**علت پیام «پاسخگو نیستم»** — دو باگ واقعی بود:
- `claude-opus-4-8` شناسهٔ معتبری نیست؛ هر کس آن را انتخاب کرده بود، هر پیامش خطا می‌گرفت. اصلاح شد به `claude-opus-5` و `Activator::retire_dead_models()` آن را روی نصب‌های موجود بازنویسی می‌کند.
- `MessageRepository::history()` پنجره را از انتهای جدید برمی‌دارد، پس در گفتگوهای بلند ممکن بود با پیام `assistant` شروع شود — که Anthropic رد می‌کند. حالا `normalise()` پیام‌های assistant ابتدایی را حذف و پیام‌های هم‌نقشِ پشت‌سرهم را ادغام می‌کند.

**عیب‌یابی** — `AiManager::LAST_ERROR_OPTION` علت آخرین خطا را نگه می‌دارد؛ تب تنظیمات آن را به‌صورت هشدار قرمز نشان می‌دهد و دکمهٔ «تست ارتباط با سرویس هوش مصنوعی» (`wp_ajax_mdr_ai_diag`) یک درخواست واقعی می‌فرستد و عین پاسخ را چاپ می‌کند. **علت خطا هرگز به مرورگر بازدیدکننده نمی‌رود** — بازدیدکننده فقط همان متن مؤدبانهٔ همیشگی را می‌بیند.

**انتقال از نسخهٔ قدیمی** — `Activator::import_legacy_install()` گزینه‌های `swc_*` و ردیف‌های هر هشت جدول را **کپی** می‌کند (نه انتقال). اجرای دوباره no-op است. ساختار دو دیتابیس بایت‌به‌بایت یکسان است، که کپی مستقیم را ایمن می‌کند.

> ⚠️ ترتیب نصب برای کاربری که نسخهٔ قدیمی دارد: **اول** مدورا را فعال کنید، **بعد** افزونهٔ قدیمی را حذف کنید. `uninstall.php` قدیمی هنگام حذف جدول‌های `swc_` را DROP می‌کند.

## نحوهٔ تست در این محیط

وردپرس روی این کانتینر قابل دانلود نیست (`wordpress.org` و آرشیوهای گیت‌هاب از پشت پراکسی ۴۰۳ می‌دهند). روش جایگزینی که جواب داده:

- استخراج فایل کلاس، stub کردن توابع وردپرس (`__`, `esc_html`, `get_option`, …) و صدا زدن متدهای private با `ReflectionMethod`
- برای `$wpdb` یک stub ساده که `SHOW TABLES LIKE` و `INSERT … SELECT` را شبیه‌سازی می‌کند
- lint همهٔ فایل‌ها با `php -l` و `node --check` برای JS
- یک اسکریپت verify که منطق autoloader را تکرار می‌کند و مطمئن می‌شود هر ارجاع namespace‌دار resolve می‌شود (باید بدهد: ۷۰ کلاس، ۰ مفقود)

## کارهای باز

- مالک محصول باید بعد از نصب ۴٫۲٫۰ دکمهٔ «تست ارتباط» را بزند؛ اگر خطایی داد، متنش تعیین می‌کند قدم بعدی چیست
- فایل ترجمه (`languages/medora.pot`) رشته‌های قدیمی دارد. چون msgidها خودشان فارسی‌اند اثری در نمایش ندارد، ولی مرتب نشده است
- `medora-cloud/` و اسناد `MEDORA-*.md` مربوط به طرح SaaS هستند و از افزونه جدا
