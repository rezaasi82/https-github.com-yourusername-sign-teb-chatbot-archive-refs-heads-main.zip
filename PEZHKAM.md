# پژکام (Pezhkam) — context

> این فایل برای شروع یک چت مستقل روی «پژکام» است. هر چیزی که برای ادامهٔ کار لازم است اینجاست.
> **مدورا به این محصول ربطی ندارد** — آن بیلد توزیع شخصی است با قوانین جدا (فایل `MEDORA.md`).

## این چیست

همان موتور چت هوشمند پزشکی، ولی بیلد **فروش در مارکت‌پلیس راست‌چین** با سیستم لایسنس.

- پوشه: `pezhkam/` · نگارش: **۱٫۰٫۰**
- Namespace: `Pezhkam\` · ثابت‌ها: `PZK_*` · پیشوند CSS: `pzk-`
- گزینه: `pzk_settings` · جدول‌ها: `{wp_prefix}pzk_*` · شورت‌کد: `[pezhkam_chat]`
- Text Domain: `pezhkam` · Author: `Pezhkam` · بدون `Plugin URI`
- نام نمایشی در پنل: **پژکام**

> رشتهٔ `pzr` فقط slug مربوط به `add_settings_error()` است و پیشوند CSS نیست — با `pzk-` اشتباه نشود.

## 🔴 قانون‌هایی که شکستنشان محصول را از کار می‌اندازد

1. **`includes/RTL_License_0949e1086a8664d9.php` هرگز نباید ویرایش شود.** فایل ionCube-encode شدهٔ مارکت است. یک بایت تغییر، sha1 را می‌شکند و افزونه اصلاً بوت نمی‌شود.
   sha1 معتبر: `7a08b73e61f2c61ca287e1f88650085a47328a54`
   این هش دقیقاً در دو جا آمده: `includes/setup.php` و `includes/core/class-plugin.php`.
2. **`includes/setup.php` همان فایلی است که برای انکد ionCube به راست‌چین فرستاده می‌شود.** گیت لایسنس داخل آن است: چک sha1 → require → `isActive()` → بوت.
3. **`Plugin::licensed()` یک لایهٔ دوم مستقل است** (defense in depth). حذفش نکنید.
4. **هیچ اطلاعات، راه ارتباطی یا لینک شخصی، در هیچ کجای محصول** — قانون صریح راست‌چین. قبلاً به همین دلیل رد شده‌ایم.
5. **هیچ ردی از نویسندهٔ AI** در فایل‌های منتشرشده. شناسهٔ مدل و لینک session فقط در trailer کامیت گیت، نه در artifact.
6. **کدهای پترن پیامکی شخصی مالک محصول هرگز نباید داخل افزونه ارسال شود.**
7. **هیچ ایموجی در هیچ فایل ارسالی** — نه کاراکتر مستقیم، نه HTML entity (`&#128172;`). آیکون فقط از `\Pezhkam\Admin\Icon`. (ایراد دور ۴)

## تاریخچهٔ نام — چرا به «پژکام» رسیدیم

| نام | سرنوشت |
|---|---|
| SignTeb Web Chat | نام اولیه، حاوی برند شخصی |
| Pazira / پذیرا | رد شد — «پذیرا» واژهٔ رایج فارسی است و محصولات ثبت‌شده دارد |
| Clinovix AI | رد شد — **دو کلمه** است (قانون: نام باید یونیک و **تک‌کلمه‌ای** باشد) و `Clinovo` یک شرکت نرم‌افزار بالینی واقعی است |
| **Pezhkam / پژکام** | فعلی — تک‌کلمه، بدون پسوند AI، در جستجو هیچ محصول یا سایتی با این نام پیدا نشد |

نام‌های بررسی و رد شده به دلیل تداخل: Tabina، Zivara، Darmika، Nikava، Sepira، Rasanik، Marhamo، Shafano، Tebrix.

## چهار دور ایراد راست‌چین و پاسخشان

**دور ۱ — امنیت و کیفیت کد**
SQL injection، ۱۴ ورودی sanitize‌نشده، ۱۳ XSS، دسترسی مستقیم به superglobal، ۲۶ فایل بدون گارد `ABSPATH`، `die/exit`، ۶۶ کلاس بدون namespace، تگ بستهٔ PHP، کامنت‌های اضافی، UI ضعیف، لایسنس انکدنشده، اطلاعات شخصی.
→ همه برطرف: مهاجرت کامل namespace، لایهٔ ورودی متمرکز، `$wpdb->prepare()` با bind، escape خروجی، بازطراحی کامل پنل، حذف اطلاعات شخصی.

**دور ۲ — نام و دارایی‌های صفحهٔ محصول**
نام باید یونیک و تک‌کلمه‌ای باشد؛ اینفوگرافیک ۹۶۰px با تصاویر واقعی داخل تگ `<img>`؛ کاور و آیکون نامناسب.

**دور ۳ — «هیچکدام از موارد قبلی برطرف نشده‌اند»**
→ برطرف شد:
- نام تک‌کلمه‌ای بدون پسوند AI
- اینفوگرافیک ۹۶۰×۵۹۲۷ از اسکرین‌شات‌های واقعی، با تگ `<img width="960">` در متن توضیحات
- هدر تبلیغاتی صفحهٔ پیش‌نمایش و همهٔ CTAهای خرید حذف شد
- لینک شخصی `signteb.com` که در فوتر پیش‌نمایش مانده بود حذف شد (منشأ اصلی ایراد «لینک شخصی» همین بود)
- کاور بازطراحی شد: زیرعنوان کوتاه و سه ویژگی کوتاه، خوانا در سایز کوچک
- ساختار ارسال فایل مطابق استاندارد

**دور ۴ — ظاهر، فونت، نسخه‌گذاری** (کامیت `42285fd`)
ایرادها: ایموجی در همه‌جا؛ استایل پیش‌فرض وردپرس و UI ناهمگون؛ «بخش‌هایی بدون فونت»؛ هدر شخصی؛ نسخه باید به `1.0.0` برگردد و از حالت خودکار خارج شود؛ اینفوگرافیک ۹۶۰px واکنش‌گرا.
→ برطرف شد:

- **ایموجی: ۲۲۸ مورد در ۲۸ فایل → صفر.** دو موردشان به‌صورت HTML entity پنهان بودند (`&#128172;` و `&#128203;` در `class-page-header.php`) که جستجوی متنی پیدایشان نمی‌کند. جایگزین: کلاس `\Pezhkam\Admin\Icon` با ۱۷ آیکون SVG + `pzkChannelIcon()` در `widget.js` + ست آیکون لندینگ.
- **علت واقعی «بدون فونت»:** `class-widget.php` لود فونت را پشت `file_exists('assets/fonts/vazirmatn.woff2')` گذاشته بود و آن فایل **هرگز وجود نداشت**؛ `admin.css` هم اساساً `font-family` نداشت. حالا وزیرمتن variable همراه بسته است.
- **استایل وردپرس:** `.widefat`/`.striped`، `.subsubsub`، `.tablenav`، notice‌ها و فیلدهای فرم به design system منتقل شدند.
- **نسخه:** `1.0.0` در هدر، `PZK_VERSION`، README و POT. `Schema::DB_VERSION` هم از `3.4.0` به `1.0.0` برگشت. هیچ bump خودکاری در کد نیست.
- **پیش‌نمایش:** `#medora-landing` (۱۴۶ رخداد)، کلاس‌های `swcl-*` و مسیر `/uploads/medora/` پاک شدند؛ و شورت‌کد غلط `[medora_chat]` که به خریدار نشان داده می‌شد اصلاح شد (در نسخهٔ گوتنبرگ به شکل `&#91;medora_chat&#93;` انکد شده بود).
- **اینفوگرافیک:** دقیقاً ۹۶۰px با `box-sizing:border-box`، بدون سرریز افقی در ۱۴۴۰/۱۰۲۴/۴۸۰/۳۶۰.

## قواعدی که از دور ۴ به بعد باید رعایت شوند

- **آیکون جدید لازم شد؟** یک path به `Icon::PATHS` اضافه کنید، نه یک ایموجی.
- **خروجی `Icon::svg()` را هرگز از `wp_kses_post` رد نکنید** — تگ `<svg>` را حذف می‌کند. مستقیم echo شود (markup ثابت است و داده‌ی کاربر ندارد). فقط `Icon::lead_dot()` که `span`+`i` می‌دهد از kses عبور می‌کند.
- **`widget.css` را وابسته به handle `pzk-font` نکنید.** وقتی گزینهٔ `use_bundled_font` خاموش است آن handle ثبت نمی‌شود و وردپرس استایلِ وابسته به یک dependency غایب را **کاملاً حذف می‌کند** — ویجت بی‌استایل می‌شود. فونت داخل `var(--pzk-font, …)` fallback کامل دارد.
- **فونت:** `assets/fonts/vazirmatn-variable.woff2` (۱۰۹KB، وزن ۱۰۰–۹۰۰) به‌همراه `OFL.txt`. حذف `OFL.txt` نقض مجوز فونت است.
- **رشته‌های ترجمه را که عوض کردید، POT را بازتولید کنید** (اسکریپت در `## نحوهٔ تست`).

## دارایی‌های صفحهٔ محصول

```
build.sh                            ساخت + اعتبارسنجی سه فایل تحویلی
marketing/
├── PACKAGING-CHECKLIST.md          چک‌لیست ارسال — قبل از هر آپلود بخوانید
├── pezhkam-cover-2100x1040.png     کاور
├── pezhkam-icon-320.png            آیکون
├── pezhkam-infographic-960.png     اینفوگرافیک (۹۶۰×۵۹۲۷)
├── product-description.html        متن توضیحات — قبل از انتشار INFOGRAPHIC_URL_HERE را عوض کنید
├── screenshots/                    ۷ اسکرین‌شات واقعی
└── guide/
    ├── pezhkam-guide.html / .pdf   راهنمای کامل فارسی
    └── pre-install-notes.txt       نکات قبل از نصب
preview/
├── index.html                      صفحهٔ پیش‌نمایش مستقل
├── wordpress-block.html            همان صفحه برای Paste در گوتنبرگ (id: pezhkam-landing)
└── images/                         تصاویر پیش‌نمایش
pezhkam/assets/fonts/               وزیرمتن variable + OFL.txt
```

**استاندارد ارسال فایل راست‌چین**
- فیلد پکیج محصول → `dist/pezhkam-package.zip` (افزونه + راهنما + نکات قبل از نصب)
- فیلد فایل بروزرسان → `dist/pezhkam.zip` (فقط افزونه)
- فایل راهنما → `pezhkam-guide.pdf`

ساخت مجدد — **دستی زیپ نکنید**:
```bash
bash build.sh
```
اسکریپت ۸ بررسی قبل از ساخت (sha1 لایسنس، هش در هر دو گیت، یکدستی نسخه، نبود ایموجی، نبود برند قبلی، وجود فونت و OFL، `php -l`، وجود PDF و نکات) و ۳ بررسی بعد از ساخت (هش لایسنس داخل هر زیپ، نبود `.DS_Store`/`__MACOSX`/`.git`) انجام می‌دهد و اگر هرکدام رد شود **هیچ فایلی نمی‌سازد**. `dist/` در `.gitignore` است.

## وضعیت فعلی

**بازخورد دور ۴ اعمال و پوش شد** — کامیت `42285fd` روی برنچ `claude/signteb-web-chat-plugin-3e4a9w`.

کارهای دستی باقی‌مانده قبل از ارسال مجدد:
1. در `marketing/product-description.html` مقدار `INFOGRAPHIC_URL_HERE` را با آدرس واقعی اینفوگرافیک آپلودشده عوض کنید (به استایل `<img>` دست نزنید).
2. `includes/setup.php` را برای انکد ionCube به راست‌چین بفرستید.
3. `bash build.sh` را روی ماشین خودتان اجرا کنید تا زیپ‌ها از آخرین وضعیت ساخته شوند.

## نکته دربارهٔ اسکرین‌شات‌ها

اسکرین‌شات‌ها از یک نصب واقعی وردپرس گرفته شده‌اند، ولی چون در این محیط وردپرس قابل دانلود نیست، برند داخلشان با PIL بازنویسی شده (کادر متن با گرادیان پس‌زمینه پر و متن تازه با فونت Vazirmatn رندر شده) و حاشیه‌هایشان تریم شده. اگر نام دوباره عوض شود، همین کار باید تکرار شود.

> اسکرین‌شات‌های فعلی **قبل از** بازطراحی دور ۴ گرفته شده‌اند و هنوز ایموجی و فونت قدیمی دارند. اگر راست‌چین دوباره ایراد ظاهری گرفت، اول اینها باید از نصب به‌روز بازگرفته شوند.

## نحوهٔ تست در این محیط

وردپرس قابل دانلود نیست (۴۰۳ از پشت پراکسی). روش جایگزین: stub کردن توابع وردپرس و صدا زدن متدها با Reflection، به‌علاوهٔ `php -l` روی همهٔ فایل‌ها و اسکریپتی که منطق autoloader را تکرار می‌کند (باید بدهد: **۷۱ کلاس، ۰ مفقود** — با افزوده‌شدن `Icon` از ۷۰ به ۷۱ رسید).

بررسی ظاهر بدون وردپرس: یک صفحهٔ mock با `admin.css` و `fonts/font.css` واقعی بسازید و با Chromium بدون هد از `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` (Playwright سراسری در `/opt/node22/lib/node_modules`) اسکرین‌شات بگیرید. همین روش برای `preview/index.html` و تست واکنش‌گرایی اینفوگرافیک در چند viewport کار می‌کند.

بازتولید POT بعد از تغییر رشته‌ها: استخراج `__`, `_e`, `_n`, `esc_html__`, `esc_html_e`, `esc_attr__`, `esc_attr_e` با text domain `pezhkam` از کل `pezhkam/**/*.php` به‌علاوهٔ سه فیلد هدر افزونه (Plugin Name / Description / Author). خروجی فعلی: ۴۱۶ ورودی.
