# پژکام (Pezhkam) — context

> این فایل برای شروع یک چت مستقل روی «پژکام» است. هر چیزی که برای ادامهٔ کار لازم است اینجاست.
> **مدورا به این محصول ربطی ندارد** — آن بیلد توزیع شخصی است با قوانین جدا (فایل `MEDORA.md`).

## این چیست

همان موتور چت هوشمند پزشکی، ولی بیلد **فروش در مارکت‌پلیس راست‌چین** با سیستم لایسنس.

- پوشه: `pezhkam/` · نگارش: **۱٫۰٫۰**
- Namespace: `Pezhkam\` · ثابت‌ها: `PZK_*` · پیشوند CSS: `pzr-`
- گزینه: `pzk_settings` · جدول‌ها: `{wp_prefix}pzk_*` · شورت‌کد: `[pezhkam_chat]`
- Text Domain: `pezhkam` · Author: `Pezhkam` · بدون `Plugin URI`
- نام نمایشی در پنل: **پژکام**

## 🔴 قانون‌هایی که شکستنشان محصول را از کار می‌اندازد

1. **`includes/RTL_License_0949e1086a8664d9.php` هرگز نباید ویرایش شود.** فایل ionCube-encode شدهٔ مارکت است. یک بایت تغییر، sha1 را می‌شکند و افزونه اصلاً بوت نمی‌شود.
   sha1 معتبر: `7a08b73e61f2c61ca287e1f88650085a47328a54`
   این هش دقیقاً در دو جا آمده: `includes/setup.php` و `includes/core/class-plugin.php`.
2. **`includes/setup.php` همان فایلی است که برای انکد ionCube به راست‌چین فرستاده می‌شود.** گیت لایسنس داخل آن است: چک sha1 → require → `isActive()` → بوت.
3. **`Plugin::licensed()` یک لایهٔ دوم مستقل است** (defense in depth). حذفش نکنید.
4. **هیچ اطلاعات، راه ارتباطی یا لینک شخصی، در هیچ کجای محصول** — قانون صریح راست‌چین. قبلاً به همین دلیل رد شده‌ایم.
5. **هیچ ردی از نویسندهٔ AI** در فایل‌های منتشرشده. شناسهٔ مدل و لینک session فقط در trailer کامیت گیت، نه در artifact.
6. **کدهای پترن پیامکی شخصی مالک محصول هرگز نباید داخل افزونه ارسال شود.**

## تاریخچهٔ نام — چرا به «پژکام» رسیدیم

| نام | سرنوشت |
|---|---|
| SignTeb Web Chat | نام اولیه، حاوی برند شخصی |
| Pazira / پذیرا | رد شد — «پذیرا» واژهٔ رایج فارسی است و محصولات ثبت‌شده دارد |
| Clinovix AI | رد شد — **دو کلمه** است (قانون: نام باید یونیک و **تک‌کلمه‌ای** باشد) و `Clinovo` یک شرکت نرم‌افزار بالینی واقعی است |
| **Pezhkam / پژکام** | فعلی — تک‌کلمه، بدون پسوند AI، در جستجو هیچ محصول یا سایتی با این نام پیدا نشد |

نام‌های بررسی و رد شده به دلیل تداخل: Tabina، Zivara، Darmika، Nikava، Sepira، Rasanik، Marhamo، Shafano، Tebrix.

## سه دور ایراد راست‌چین و پاسخشان

**دور ۱ — امنیت و کیفیت کد**
SQL injection، ۱۴ ورودی sanitize‌نشده، ۱۳ XSS، دسترسی مستقیم به superglobal، ۲۶ فایل بدون گارد `ABSPATH`، `die/exit`، ۶۶ کلاس بدون namespace، تگ بستهٔ PHP، کامنت‌های اضافی، UI ضعیف، لایسنس انکدنشده، اطلاعات شخصی.
→ همه برطرف: مهاجرت کامل namespace، لایهٔ ورودی متمرکز، `$wpdb->prepare()` با bind، escape خروجی، بازطراحی کامل پنل، حذف اطلاعات شخصی.

**دور ۲ — نام و دارایی‌های صفحهٔ محصول**
نام باید یونیک و تک‌کلمه‌ای باشد؛ اینفوگرافیک ۹۶۰px با تصاویر واقعی داخل تگ `<img>`؛ کاور و آیکون نامناسب.

**دور ۳ — «هیچکدام از موارد قبلی برطرف نشده‌اند»**
→ برطرف شد:
- نام تک‌کلمه‌ای بدون پسوند AI
- اینفوگرافیک ۹۶۰×۵۹۲۷ از اسکرین‌شات‌های واقعی، با تگ `<img width="960">` در متن توضیحات
- هدر تبلیغاتی صفحهٔ پیش‌نمایش و همهٔ CTAهای خرید حذف شد
- لینک شخصی `signteb.com` که در فوتر پیش‌نمایش مانده بود حذف شد (منشأ اصلی ایراد «لینک شخصی» همین بود)
- کاور بازطراحی شد: زیرعنوان کوتاه و سه ویژگی کوتاه، خوانا در سایز کوچک
- ساختار ارسال فایل مطابق استاندارد

## دارایی‌های صفحهٔ محصول

```
marketing/
├── pezhkam-cover-2100x1040.png    کاور
├── pezhkam-icon-320.png            آیکون
├── pezhkam-infographic-960.png     اینفوگرافیک (۹۶۰×۵۹۲۷)
├── product-description.html        متن توضیحات — قبل از انتشار INFOGRAPHIC_URL_HERE را عوض کنید
├── screenshots/                    ۷ اسکرین‌شات واقعی
└── guide/
    ├── pezhkam-guide.html / .pdf   راهنمای کامل فارسی
    └── pre-install-notes.txt       نکات قبل از نصب
preview/                            صفحهٔ پیش‌نمایش (بدون هدر تبلیغاتی، بدون لینک)
```

**استاندارد ارسال فایل راست‌چین**
- فیلد پکیج محصول → `dist/pezhkam-package.zip` (افزونه + راهنما + نکات قبل از نصب)
- فیلد فایل بروزرسان → `dist/pezhkam.zip` (فقط افزونه)
- فایل راهنما → `pezhkam-guide.pdf`

ساخت مجدد:
```bash
zip -rq dist/pezhkam.zip pezhkam -x '*.DS_Store'
```

## وضعیت فعلی

**در انتظار تأیید راست‌چین.** تصمیم صریح مالک محصول: تا تأیید نشدن، **هیچ تغییری روی پژکام داده نشود**؛ آپدیت بعد از تأیید.

## نکته دربارهٔ اسکرین‌شات‌ها

اسکرین‌شات‌ها از یک نصب واقعی وردپرس گرفته شده‌اند، ولی چون در این محیط وردپرس قابل دانلود نیست، برند داخلشان با PIL بازنویسی شده (کادر متن با گرادیان پس‌زمینه پر و متن تازه با فونت Vazirmatn رندر شده) و حاشیه‌هایشان تریم شده. اگر نام دوباره عوض شود، همین کار باید تکرار شود.

## نحوهٔ تست در این محیط

وردپرس قابل دانلود نیست (۴۰۳ از پشت پراکسی). روش جایگزین: stub کردن توابع وردپرس و صدا زدن متدها با Reflection، به‌علاوهٔ `php -l` روی همهٔ فایل‌ها و اسکریپت verify که منطق autoloader را تکرار می‌کند (باید بدهد: ۷۰ کلاس، ۰ مفقود).

رندر تصاویر با Chromium بدون هد از مسیر `/opt/pw-browsers/chromium-1194/chrome-linux/chrome` و Playwright سراسری در `/opt/node22/lib/node_modules`.
