# این اسکریپت‌ها فقط باید از طریق CLI اجرا شوند، نه از طریق مرورگر
<FilesMatch "\.php$">
    <IfModule mod_authz_core.c>
        Require all denied
    </IfModule>
    <IfModule !mod_authz_core.c>
        Order Allow,Deny
        Deny from all
    </IfModule>
</FilesMatch>
