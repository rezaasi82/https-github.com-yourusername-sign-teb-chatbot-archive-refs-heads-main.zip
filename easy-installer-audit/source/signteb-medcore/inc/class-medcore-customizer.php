<?php
/**
 * SignTeb MedCore — Customizer (منسوخ)
 *
 * این ماژول با فریم‌ورک جدید Design Tokens جایگزین شده است:
 *   inc/src/Customize/DesignTokens.php  — تعریف توکن‌ها + خروجی CSS زنده
 *   inc/src/Customize/Customizer.php     — ثبت کنترل‌ها + پیش‌نمایش زنده
 *
 * دیگر در functions.php بارگذاری نمی‌شود و اینجا فقط برای سازگاری باقی مانده
 * (بدون ثبت هیچ هوکی تا با نسخه‌ی جدید تداخل نکند).
 *
 * @package SignTeb_MedCore
 * @deprecated 1.0.7 استفاده از SignTeb\MedCore\Customize\Customizer
 */

defined( 'ABSPATH' ) || exit;

// عمداً خالی — منطق به SignTeb\MedCore\Customize منتقل شده است.
