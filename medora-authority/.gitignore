/vendor/
/node_modules/
/assets/js/*.js
/assets/js/*.asset.php
/assets/js/*.map
/assets/css/*.css
# Compiled catalogues: sources are the .po files, builds ship in the release
# artifact. `composer i18n:build` regenerates both.
/languages/*.mo
/languages/*.json
# Release artifacts. `composer package` rebuilds them; CI publishes them.
/dist/
composer.lock
package-lock.json
.phpunit.cache/
.phpunit.result.cache
.phpcs-cache
*.log
.DS_Store
